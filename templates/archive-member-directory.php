<?php
/**
 * Template: Member Directory Archive
 *
 * Scaffold only — no card rendering, no filters, no PMP yet.
 * TemplateLoader routes member-directory archive requests here instead of
 * the active theme template.
 */

defined( 'ABSPATH' ) || exit;

get_header();
?>

<div class="memdir-directory">
	<p>Member Directory &mdash; Archive</p>
</div>

<?php get_footer();
