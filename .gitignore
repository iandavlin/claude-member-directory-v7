# Section pointers — managed per-environment via AdminSync UI
sections/

# WordPress / PHP
*.log
.DS_Store
Thumbs.db

# Node (if ever used for tooling)
node_modules/

# Root-level patch scripts (already applied; tools/ has the keepers)
/patch-*.js

# Backup archives
*.tar.gz

# Claude local settings
.claude/settings.local.json
